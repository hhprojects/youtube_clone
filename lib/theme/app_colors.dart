import 'package:flutter/material.dart';

class TColorTheme {
  // Base colors
  static const backgroundColor = Color(0xFF1F1B24);
  static const primaryColor = Colors.teal;
  
  // Text colors
  static const textPrimary = Colors.white;
  static const textSecondary = Color(0xFF9E9E9E);
  
  // UI Element colors
  static final searchBarFill = Colors.white.withOpacity(0.1);
  static const bottomNavUnselected = Colors.grey;
  
  // Prevent instantiation
  TColorTheme._();
} 